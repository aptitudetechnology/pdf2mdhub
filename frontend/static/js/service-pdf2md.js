// ~/pdf2mdhub/frontend/static/js/src/service-pdf2md.js

/**
 * Dummy function to simulate PDF to Markdown conversion using 'opendocsg/pdf2md' logic.
 * This is a placeholder until the real implementation is available.
 * @param {File} file - The PDF file object.
 * @param {Object} metadata - Any additional metadata for the conversion.
 * @returns {Promise<Object>} - A promise resolving to an object with markdown, success, and message.
 */
export async function convertWithPdf2md(file, metadata) {
    console.log("--- Dummy convertWithPdf2md called ---");
    console.log("Input File:", file ? file.name : "[No File]");
    console.log("Input Metadata:", metadata);

    // Simulate an asynchronous operation (e.g., network request, heavy computation)
    await new Promise(resolve => setTimeout(resolve, 1000)); // Wait for 1 second

    const dummyMarkdown = `# Converted from ${file ? file.name : 'Unknown PDF'}

This is **dummy Markdown content** generated by the placeholder \`service-pdf2md.js\` file.

## Metadata Received:
\`\`\`json
${JSON.stringify(metadata, null, 2)}
\`\`\`

Hopefully, your actual PDF to Markdown conversion will be much more exciting!`;

    // Return a dummy result structure
    return {
        markdown: dummyMarkdown,
        success: true,
        message: `Dummy conversion successful for ${file ? file.name : 'unknown file'}.`
    };
}

// You might also have other exports here if needed by upload.js or other files
// export function anotherDummyFunction() {
//     console.log("Another dummy function called!");
// }