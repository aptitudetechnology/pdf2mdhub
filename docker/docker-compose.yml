version: '3.8'
services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ../backend:/app/backend
      - ../frontend:/app/frontend
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
